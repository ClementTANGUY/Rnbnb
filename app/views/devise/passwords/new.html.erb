<div class="row d-flex justify-content-center">
	<div class="col-md-4 col-md-offset-4">
		<h2>Mot de passe oublié ?</h2>

		<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>

			<div class="form-group">
				<p>Entrez votre email de connexion sur R'n'Bnb afin que nous puissions vous envoyer les instrictions de réinitialisation de votre mot de passe.</p>
			  <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control', placeholder: "L'Email de votre compte R'n'BnB" %>
			</div>

			<div class="form-group">
			  <%= f.submit "Recevoir le mail de réinitialisation", class: 'btn btn-primary' %>
			</div>
		<% end %>

	</div>
</div>
<%= render 'shared/devise_errors' %>