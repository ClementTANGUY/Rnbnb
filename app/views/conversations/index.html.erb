<div class="row">

     <div class="col-md-12">

        <div class="card card-default">

            <div class="card-header">Boîte de réception</div>

            <div class="card-body">

                <div class="container">

                    <% @conversations.each do |conversation| %>

                    <% other = conversation.sender == current_user ? conversation.recipient : conversation.sender %>

                    <%= link_to conversation_messages_path(conversation) do %>

                          <div class="row conversation">

                               <div class="col-md-12">

                                   <%= image_tag avatar_url(other), class: "img-circle avatar-medium" %>

                               </div>

                               <div class="col-md-12">

                                    <%= other.fullname %><br>

                                   <%= timeago_tag conversation.messages.last.created_at, :lang=>:fr%>
                               </div>

                               <div class="col-md-12">

                                    <%= conversation.messages.last.content %>

                           </div>

                     <% end %>

               <% end %>

           </div>

        </div>

     </div>

    </div>

 </div>
